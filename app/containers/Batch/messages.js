/*
 * Batch Messages
 *
 * This contains all the text for the Batch container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Batch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Batch container!',
  },
});
