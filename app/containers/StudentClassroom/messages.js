/*
 * StudentClassroom Messages
 *
 * This contains all the text for the StudentClassroom container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StudentClassroom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentClassroom container!',
  },
});
