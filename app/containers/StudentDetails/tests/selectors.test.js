// import { selectStudentDetailsDomain } from '../selectors';

describe('selectStudentDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
