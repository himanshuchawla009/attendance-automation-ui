/*
 * Class Messages
 *
 * This contains all the text for the Class container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Class';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Class container!',
  },
});
