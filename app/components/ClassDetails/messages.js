/*
 * ClassDetails Messages
 *
 * This contains all the text for the ClassDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ClassDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ClassDetails component!',
  },
});
