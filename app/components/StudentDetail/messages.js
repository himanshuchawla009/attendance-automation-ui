/*
 * StudentDetail Messages
 *
 * This contains all the text for the StudentDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StudentDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentDetail component!',
  },
});
