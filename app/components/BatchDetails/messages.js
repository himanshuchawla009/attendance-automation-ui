/*
 * BatchDetails Messages
 *
 * This contains all the text for the BatchDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BatchDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BatchDetails component!',
  },
});
