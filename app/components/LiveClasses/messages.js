/*
 * LiveClasses Messages
 *
 * This contains all the text for the LiveClasses component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LiveClasses';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LiveClasses component!',
  },
});
